.wrapper_with_padding
  #docs.clearfix
    - unless @docs.blank?
      - @docs.each do |doc|
        .doc
          %p.title= link_to doc.title, doc
          %p.date= time_ago_in_words(doc.created_at)
          %p.content= truncate(doc.content, length:50)
    - else
      %h2 Create Doc!
      %p Start creating Docs and filling your account with crap
      %button= link_to 'Create Doc', new_doc_path